require 'spec_helper'

describe Prpr::ConflictLabel do
  it 'has a version number' do
    expect(Prpr::ConflictLabel::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
